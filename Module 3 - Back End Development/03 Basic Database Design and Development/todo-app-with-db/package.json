{
  "name": "todo-app-with-db",
  "version": "2.0.0",
  "description": "Todo List App v2.0 — Full-stack monorepo (React + Express + PostgreSQL)",
  "private": true,
  "scripts": {
    "dev": "concurrently -n BE,FE -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "install:all": "cd frontend && npm install && cd ../backend && npm install",
    "typecheck": "cd frontend && npx tsc --noEmit && cd ../backend && npx tsc --noEmit",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
